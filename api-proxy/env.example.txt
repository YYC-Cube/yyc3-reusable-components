# ════════════════════════════════════════════════════════════════════
# YYC³ AI Family - API Proxy Server Environment Variables
# YYC³ AI 家族 - API 代理服务环境变量模板
# ════════════════════════════════════════════════════════════════════
# Version: 0.9.4 [Personalize]
#
# 使用方式 / Usage:
#   cp env.example.txt .env
#   然后编辑 .env 填入实际值 / Then edit .env with actual values
# ════════════════════════════════════════════════════════════════════

# ──────────── PostgreSQL 连接配置 / PostgreSQL Connection ────────────
YYC3_PG_HOST=localhost
YYC3_PG_PORT=5432
YYC3_PG_DATABASE=yyc3_family
YYC3_PG_USER=yyc3_admin
YYC3_PG_PASSWORD=
YYC3_PG_SSL=prefer
YYC3_PG_POOL_SIZE=10

# ──────────── API 代理服务配置 / API Proxy Config ────────────
YYC3_PORT=3721
YYC3_CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5174
YYC3_LOG_LEVEL=info

# ──────────── 认证配置 / Authentication Config ────────────
# 留空 = 无认证（本地开发模式）/ Empty = no auth (local dev mode)
# 生产环境请设置一个强令牌 / Set a strong token for production
YYC3_AUTH_TOKEN=

# ──────────── Docker 模式 / Docker Mode ────────────
# Docker Compose 环境下自动覆盖:
#   YYC3_PG_HOST=yyc3-postgres
#   YYC3_CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://yyc3-frontend:5173
